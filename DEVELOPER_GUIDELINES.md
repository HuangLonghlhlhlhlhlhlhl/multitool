# 开发准则 (Development Guidelines)

1. **编译与打包规范**：
   - 除非收到用户明确通知或指示要求生成新应用安装包/压缩包，日常编译及调试过程中**绝不**压缩生成 app 压缩包（如 `.dmg`、`.zip` 等）。

2. **状态栏遥测信息排版对齐规范**：
   - 任务栏（Status Bar）的系统状态遥测信息两行单宽等距输出必须严格按照以下对齐示例进行字符渲染：
     ```
     C:32%  M:69% S:61%  G: 10%   F:3887  ↑11.3K 
         83°C     16° C   52°C    25°C     RPM   ↓12.0K 
     ```
   - 在 SF Mono / Menlo 等宽字体中，为实现此对齐，各遥测指标每一列的顶级与底级行填充宽度配置如下：
     - CPU: Top 左对齐 Width 7, Bottom `"  %2.0f°C"` 并右补空格至 Width 7
     - RAM: Top 左对齐 Width 7, Bottom `"  %2.0f°C"` 并右补空格至 Width 7
     - SSD: Top 左对齐 Width 7, Bottom `"  %2.0f°C"` 并右补空格至 Width 7
     - GPU: Top 左对齐 Width 8, Bottom `"  %2.0f°C"` 并右补空格至 Width 8
     - Fan: Top 左对齐 Width 8, Bottom `"   RPM"` 并右补空格至 Width 8
     - Net: Top 右对齐 Width 6, Bottom 右对齐 Width 6
   - 两行在拼接各列时，列之间使用空字符串 `""` 拼接，并在最终拼接后通过正则去除尾随空格，并消除公共前导空格，从而在保持任何开启组合下各列数字垂直对齐的同时实现无多余空白的极致紧凑排版。

3. **雷达图节点即时交互规范**：
   - 极坐标图上显示的 WiFi/蓝牙节点在鼠标悬浮时，必须无延迟地立即展示 Overlay 细节 Tooltip（展示 SSID/名称、精确测距、信号强度等），不能仅依赖系统自带的 `.help` 延迟提示。
   - Hover 的节点必须被赋予 `.zIndex(100)` 优先级，保证其信息卡片处于最顶层。
