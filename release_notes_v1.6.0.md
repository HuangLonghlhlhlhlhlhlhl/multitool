# 🚀 STATUS CTRL v1.6.0 — 一键在线升级与温度单位规范化重构

本次重大更新重点引入了**智能在线检查更新与在线自动升级机制**，并对全局温度单位进行了**规范化的 `°C` 度量衡重构**，进一步提升了 STATUS CTRL 作为 macOS 极客硬件监控工具的使用体验！

---

### 🆕 本次更新亮点 (v1.6.0)：

#### 🔄 智能在线升级系统 (Auto-Updater)
- **GitHub 接口深度联动**：设置中全新支持“立即检查更新”，秒级对接 GitHub 仓库获取最新发布分支。
- **红点呼吸提醒**：新版本来临时，主界面右上角的设置齿轮图标、以及系统设置内“检查更新”按钮旁，均会显现精美的通知红点提示。
- **后台静默下载与挂载**：支持在设置里一键“在线更新”，配备高精百分比下载进度条，下载完毕后**自动在系统级挂载并弹出 `.dmg` 安装包**，点击即可拖拽替换。
- **跳过偏好忽略**：支持“跳过此版本”偏好记忆，静默当前升级，且可随时在设置中点击“恢复提示”进行重置。
- **跨模块交互加固**：彻底打通并修复了 Dashboard 主面板远程调起 Settings 页面的 `openSettingsWindow()` 通道。

#### 🌡️ 全局温度刻度重构 (Celsius Refactoring)
- **规范化 `°C` 显示**：将菜单栏双行监控、Mock 预览以及主面板 7 路高级遥测传感器等全部温度显示，从 `°` 重构为符合国际标准的 **`°C`**。
- **等宽对齐空间微调**：通过极致地调配前置占位符（例如：`  45°` ➡️ ` 45°C`），保证字符长度严格锁死在 **5** 个字符宽度，保障状态栏在各种状态下均能实现极致的像素级对齐，杜绝界面抖动或位移。

#### 🏗️ 构建与部署优化
- **Makefile 自动连结**：构建链自动引入 `UpdateManager` 并在双切片（x86_64 + arm64）上进行胖包混合。
