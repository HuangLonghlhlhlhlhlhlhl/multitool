# STATUS CTRL (多功能小助手) v1.9.6 Release Notes

STATUS CTRL (多功能小助手) 迎来 1.9.6 重磅版本更新！在此版本中，我们正式引入了全新的「程序模型」管理选项卡，并针对系统风扇/电池的 SMC 遥测写入、大文件扫描循环、进程获取安全性以及窗口大小拉伸卡顿等问题，进行了系统级的稳定性加固与极客交互打磨。

---

## ⚡ 核心更新特性 (Core Features)

### 1. 🚀 全新「程序模型 (App & Model)」管理面板
为了帮助极客与开发者更彻底地掌控 macOS 系统空间，我们在主视图中新增了第六大选项卡：
- **应用程序残留清理 (App Leftovers)**: 自动扫描系统中的已安装应用并精准识别其可能遗留的配置、缓存和支持文件。支持选择性或一键深度清理，彻底解决 App 卸载不干净的残留顽疾。
- **终端智能助手清理 (Terminal Agents)**: 专为 AI 时代打造，可快速扫描并整理本地 `Claude Code`、`Antigravity` 等终端智能体的数据文件夹及自定义技能（Skills）/配置目录，支持清理闲置技能，让本地 AI 运行更清爽。
- **本地大模型管理 (Local LLMs)**: 深度对接本地大模型引擎（如 `Ollama` 与 `LM Studio`），自动扫描下载的 GGUF 格式及各路本地大语言模型，直观展示每个模型的磁盘占用大小，并支持单个/批量快速删除以释放数十 GB 磁盘空间。

### 2. ⚙️ SMC 遥测与控制机制高可用升级
- **Sudo 特权自动回退 (Sudoers Fallback)**: 在调节风扇目标转速、切换手动模式或设置电池电量上限（Battery Charge Limit）时，若常规底层 SMC 驱动锁出现写入受限，系统将自动安全回退至通过特权辅助工具 `smchelper` （通过 `sudo -n`）执行，确保操作 100% 成功。
- **静态硬件配置后台异步加载**: 遥测管理器 `TelemetryManager` 启动时，将风扇数量、最小/最大转速及目标速度等静态硬件指标移至后台队列异步载入缓存，避免应用启动时因 SMC 锁竞争造成主线程阻塞卡顿。

---

## 🔧 系统安全、稳定性与体验优化 (Security & Stability Audit)

### 1. 🛡️ Swift 强转 CChar 指针越界越界崩溃修复
- 修复了 `MemoryPurger` 中通过 `proc_pidinfo` 提取进程 BSD Name 时，使用指针重绑定强转 CChar 指针可能引起的主线程越界崩溃隐患。我们重构为基于 17 字节 Null-terminated 字符缓冲区的安全转换，保证在后台遍历进程时的绝对安全。

### 2. 🌀 符号链接无限循环扫描防御
- 针对大文件深度查重扫描，在文件系统枚举中增加了对符号链接（Symbolic Link）的识别。当检测到软链接时，扫描引擎将自动跳过子树，杜绝因环状符号链接造成无限递归以致进程栈溢出的崩溃隐患。

### 3. 🪟 实时窗口拉伸（Live Resize）遥测自适应防抖
- 新增对窗口开始和结束 Live Resize 状态的监听。在用户手动拉伸/调整 UI 窗口大小时，自动暂停后台遥测轮询；拉伸结束恢复。彻底消除了由于高频界面绘制与拉伸渲染冲突产生的卡顿感。

### 4. 💫 状态栏小窗口渐变过渡动画与能效调优
- 为状态栏 Mini 状态小窗口的弹出和隐藏增加了平滑的 Alpha 渐变淡入淡出动画，视觉交互更具科技美感。
- 自动追踪 Mini Window 的 active 状态，当其处于活跃且主面板关闭时，自动将轮询周期调整为更合理的能效模式（电池模式 4.0s，接电模式 2.0s），完美平衡高实时遥测与整机能效。

---

## 📦 安装与升级指南

1. **直接覆盖**: 下载 `STATUS CTRL-v1.9.6.dmg`，将 App 拖入 `/Applications` 覆盖旧版本即可。
2. **免密提权保留**: 如果您在此前版本已授权过风扇手动控制，覆盖安装后依然享有免密调速和电量限制控制，无需重复输入管理员密码。
