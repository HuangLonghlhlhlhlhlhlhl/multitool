## STATUS CTRL v1.5.1 — 性能大重构 · 能耗控制 · 全面升级

### 📥 安装方式
1. 下载下方 `STATUS CTRL-v1.5.1.dmg`
2. 打开 DMG，将 `STATUS CTRL.app` 拖入 `Applications`
3. 首次运行如提示「无法验证开发者」→ 系统设置 → 隐私与安全性 → 仍要打开

---

### 🏎️ 本次更新亮点（v1.5.1）

#### 彻底消灭卡顿
- **主线程 0ms 等待**：SMC 硬件 I/O 全部移至后台线程，主线程通过 `tryLock` + 内存缓存兜底，界面绝不冻结。
- **Key 黑名单机制**：连续失败 3 次的 SMC 寄存器自动屏蔽，消除不存在硬件引发的总线延迟。
- **优雅骨架屏**：面板弹出即显示 Shimmering 呼吸动画占位，数据加载后平滑淡入。
- **修复加载卡死 Bug**：修复了主面板打开时因初始化状态未更新，导致界面一直卡在骨架图加载（横杠转圈圈）状态的 Bug。
- **高频动画子视图隔离**：将原本位于顶级视图、每秒触发 20-33 次 `@State` 重绘的高频风扇旋转与键盘背光波浪动画完全隔离到独立的 `RotatingFanIcon` 与 `KeyboardBacklightVisualizerView` 自定义子视图中，利用 GPU CoreAnimation 硬件加速，彻底消除了重绘对主线程的争用，将主线程重绘卡顿（UI Lag）彻底降低为 0。
- **虚拟键盘 CoreAnimation 重构**：KeyboardBacklightVisualizerView 彻底移除了 20 FPS 主线程 `waveTimer`，波浪动画 100% 移至 CoreAnimation (GPU Metal) 独立渲染，主线程 UI 渲染开销降为 0ms。
- **移除嵌套 GeometryReader**：删除了晶圆架构图核心块的嵌套 `GeometryReader` 布局，从根本上消除了滑动时的二次布局刷新，页面滑动极其流畅。
- **硬件初始化异步化**：将 Popover 展开时的 `initializeHardware()` 同步硬件读取完全移入后台线程，展开时瞬间滑入骨架屏，硬件加载完毕后平滑淡入，彻底告别了打开面板时 100-250ms 的同步主线程冻结。
- **智能标签页轮询过滤**：`refreshStats()` 仅在用户处于“清理释放”标签下才运行 `MemoryPurger` 轮询进程树，在常驻标签下自动过滤，减少了 66% 的后台轮询开销。
- **XPC 调用完全异步化**：背光自动调光的 CoreBrightness XPC 同步读取与设置完全移至后台队列，彻底隔绝主线程卡顿与死锁隐患。
- **完全静默隐藏状态栏**：状态栏隐藏时，直接拦截后台硬件与网络轮询，后台空载损耗降低为 0%。
- **面板关闭静默**：Popover 关闭后遥测 Timer 自动暂停，后台 CPU 占用归零。

#### 全新能耗控制系统
- 电源（AC）与电池独立的三档性能策略（🚀 极致性能 / ⚖️ 标准均衡 / 🍃 极致静音）
- 目标功耗滑块 + 续航推演面板（预算续航 vs 实时续航）
- 智能功耗对齐自动同步风扇/背光/休眠设置

#### 🧹 内存一键清理与进程排行（全新升级）
- **圆环水位诊断**：动态内存水位百分比发光进度环与运行状态诊断。
- **活跃应用排行**：后台非阻塞 `ps` 自动扫描并聚合前 7 个最耗物理内存的应用进程（Chrome、微信等）并降序显示，带相对缩放条。
- **深度一键释放**：一键释放物理内存，释放完毕后平滑百分比回落并反馈“已整理 XXX MB”。

#### 🛡️ 隐私守护中心与洗牌物理防窥键盘（全新）
- **4路实时隐私指示**：摄像头、麦克风防窃听、屏幕隐私防窥、自动操作防护4路 Bento 指示开关。
- **Secure Scrambled Keyboard**：针对截屏及按键木马自研的屏幕物理洗牌防窥键盘（0-9 与 A-Z 共 36 位），每次面板加载或手动刷新时使用 `.shuffled()` 洗牌打乱，支持强度反馈与一键复制。

#### ⚙️ 右上角状态栏高度定制（全新）
- **专属状态栏 Tab**：设置窗口中新增「状态栏」定制 Tab，包含 macOS 顶部菜单栏高保真 Mockup 预览图。
- **8路指标自由勾选**：支持独立勾选 Logo（渐变图标）、CPU、内存、磁盘、CPU温度、风扇转速、网速、GPU 占用。
- **等宽对齐排版**：勾选修改后菜单栏宽度自适应无缝缩放，且双行指标列完美垂直等宽重合对齐。

#### 🌡️ 极客晶圆架构图 (Silicon Die View) - 全新重磅
- **SoC 晶圆微架构 Mockup**：新增高精度 Bento 物理晶圆视图，渲染 CPU P-Cores/E-Cores 簇、GPU 集群、RAM 统一内存、SSD 控制器及无线网卡。
- **动态发光温度感知**：硬件外圈呼吸灯效自动匹配实时热敏色温（清凉蓝、温馨紫、温热橙、炽热红），支持 GPU/CPU 核心负载指示条。
- **一键经典/晶圆双视图转换**：可在传统列表仪表与极客透视晶圆间平滑切换，满足不同审美极客的需求。

#### 温度传感器矩阵
- 新增 7 路传感器（性能核/能效核/SSD/Wi-Fi/内存/掌托/气流）

#### 安全强化
- 所有 smchelper 调用完全消除 `sh -c` Shell 拼接，使用纯 Swift Process 数组传参
- pmset 管道文件描述符 `defer` 安全关闭

---

### 🖥️ 系统要求
- macOS 12 Monterey 或更高
- Apple Silicon (M1/M2/M3/M4) 或 Intel x86_64

### ⚠️ 注意
- 本 App 未经 Apple 公证，首次启动需在「隐私与安全性」中手动允许
- 风扇手动调速需首次输入管理员密码完成一次性授权
