# STATUS CTRL (多功能小助手) v1.9.3 Release Notes

STATUS CTRL (多功能小助手) 迎来重磅跨代升级！在 v1.9.3 版本中，我们彻底重构了底层窗口呈现架构，引入了完全独立的毛玻璃浮动面板与系统级交通灯控制；同时，我们带来了全新的 **“空间物理定位雷达”** 与键盘步进位移引擎，开启了硬件感知交互的全新篇章。

---

## ⚡ 核心更新特性 (Core Features)

### 1. 独立毛玻璃面板与自由拉伸窗口 (Translucent Resizable NSWindow)
- **告别 NSPopover 局限性**：彻底舍弃传统临时气泡，用高度定制的独立 `NSWindow` 承载仪表盘面板。
- **Cocoa 原生三色交通灯**：集成标准左上角红黄绿交通灯，全面支持窗口最小化、全屏及关闭。
- **透明标题栏与玻璃拟态 (Glassmorphism)**：融合 HUD 极客毛玻璃视觉材料 (`.hudWindow` + `.behindWindow`)，实现三色灯悬浮于内容之上的现代高级质感。
- **鼠标背景拖拽与无缝缩放**：支持通过拖拽背景任意移动窗口，并允许自由拉伸窗口边界，完美自适应任何高宽尺寸。
- **双模开辟体验**：
  1. 点击状态栏左键：在图标正下方弹出对齐的悬浮窗口。
  2. 右键菜单选择 “打开主面板”：在屏幕中央打开完全脱离状态栏的独立中央主面板。

### 2. 空间物理定位雷达与无线连接拆分 (Spatial Telemetry Radar Dashboard)
- **UI 模块化物理拆分**：将拥挤的无线页面划分为两个清晰的二级导航子页面：
  - **`📡 空间物理定位雷达`**：提供以您为中心的极客风环形物理分布探测仪表盘。
  - **`⚡ 流量监控与高可用测速`**：包含一键多节点测试与活跃网络进程 Top 10 排行榜。
- **全向罗盘度盘 (Compass Rose cardinal directions)**：雷达圆盘外围集成精致的罗盘度盘，标识 `N, S, E, W` 方位并带刻度。
- **三角几何方位逆旋转**：应用反向三角几何变换公式，当用户调整航向或行走移动时，信号源在雷达图上的物理相对方位和测距能够实时重算并绝对锁定，提供“人动图动”的高精度罗盘对齐效果。
- **全向键盘步进与航向微调**：
  - 支持键盘按 `W / A / S / D` 或 `↑ / ↓ / ← / →` 方向键模拟您的物理行走轨迹（实时更新坐标和物理距离 badge）。
  - 支持按 `Q / E` 微调电脑朝向偏角。
  - 支持按 `R` 一键重置物理位移与航向归零，按 `+ / -` 实现雷达图焦距无级变焦。

### 3. 系统级抗崩、稳定审计与架构优化 (Stability Audits)
- **死锁防御 (Deadlock Prevention)**：全面审计 SMC 传感器寄存器锁释放路径，通过 defer 机制加固，避免高并发读取硬件指标时的阻塞与死锁。
- **零 Force Unwraps 加固**：清理内存深度净化和垃圾文件解析模块的崩溃隐患，确保所有路径均为防空安全策略。
- **完全双架构编译**：适配最新的 Apple Silicon (`arm64`) 与 Intel (`x86_64`) 芯片架构，完美支持 macOS 12.0+。

---

## 🎨 视觉体验与人机工学 (Ergonomics)
- 配备清晰的 Telemetry HUD 数字化看板：直观呈现 `🧭 航向角: 045° 东北` 与 `📍 物理坐标: X: 1.5m, Y: -0.5m`。
- 新增雷达扫描区域的动态半透明激光旋转扫光与发射台脉冲动画，大大增强极客空间质感。
- 无线/蓝牙设备卡片加入了更加柔和的呼吸微光与高刷新物理测距章。

---

感谢您对 **STATUS CTRL** 的支持！本版本凝聚了对于 macOS 原生交互与空间感知的执着追求，为您奉上极客专属的多功能桌面助理。
